<html>
<head>
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
</head>
<body>
    <p>Pokemons en la lista</p>
    <select id="dynamic_select">
    	<option value="" selected>Elige un pokemon</option>
    {{#pokemons}}
        <option value="http://127.0.0.1:8080/pokemon?name={{name}}">{{name}}</option> 
    {{/pokemons}}
    </select>
    <button href="http://127.0.0.1:8080/">Pincha aquí para volver a la página principal</button>

	<script>
	    $(function(){
	      // bind change event to select
	      $('#dynamic_select').on('change', function () {
	          var url = $(this).val(); // get selected value
	          if (url) { // require a URL
	              window.location = url; // redirect
	          }
	          return false;
	      });
	    });
	</script>
</body>
</html>